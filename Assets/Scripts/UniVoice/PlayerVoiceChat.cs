using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UniVoice;

public class PlayerVoiceChat : MonoBehaviour
{
    /*public static VoiceChatManager Instance;

    private VoiceNetwork voiceNetwork;

    private void Awake()
    {
        if (Instance == null)
            Instance = this;
        else
            Destroy(gameObject);

        DontDestroyOnLoad(gameObject);
    }

    private void Start()
    {
        // Initialize UniVoice
        voiceNetwork = VoiceNetwork.Instance;
        voiceNetwork.Initialize();
    }

    // Method to start transmitting voice
    public void StartTransmitting()
    {
        voiceNetwork.StartTransmitting();
    }

    // Method to stop transmitting voice
    public void StopTransmitting()
    {
        voiceNetwork.StopTransmitting();
    }*/
}
